homepage displays top 4 projects chosen by me 

click image tile to live demo 

demo page has all four smaller tiles side by side (picker)

then main component under 



home page sends data to second page 

component tells the picker list which one is highlighted


picker list can be clicked to display below 




when i click on a project tile - change display on all project tiles to hidden 
and show display of the clicked projecttile 



const [visible, setVisible] = React.useState(false);

function click(e) {
  e.preventDefault();
  setVisible(true);
}

return (
  <div>
    <button onClick={(event) => {
       click(event)
    }} />
    
    {isVisible && (
      <YourComponent />
      )
    }
  </div>
)